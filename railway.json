{
    "build": {
      "builder": "nixpacks",
      "buildCommand": "composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev && php artisan config:cache && php artisan route:cache && php artisan storage:link && chmod +x start.sh"
    },
    "deploy": {
      "startCommand": "./start.sh",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }