{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apk add apache2 && composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev && php artisan config:cache && php artisan route:cache && php artisan storage:link"
  },
  "deploy": {
    "startCommand": "/usr/sbin/apachectl -D FOREGROUND",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
